# Repository Instructions

- Keep changes focused on infrastructure, platform, and security automation.
- Prefer declarative configuration and avoid ad hoc scripts unless they are validation helpers.
- Match the architecture in `docs/architecture.md` when adding files or modules.
- Keep YAML, Terraform, and PowerShell examples minimal but valid.
- Use environment overlays for dev, staging, and production differences.
