# Cluster Module

Defines the Kubernetes execution model for local Minikube-based profiles.

The module emits cluster identity, target contexts, and GitOps target scopes so higher layers can deploy to a single namespace model or profile-per-environment model.
